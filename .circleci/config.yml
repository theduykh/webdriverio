version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: 'npm install'
  test:
    docker:
       - image: circleci/node:4.8.2
    steps:
      - run_test
      - run: 'npm test wdio.conf.js'
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
